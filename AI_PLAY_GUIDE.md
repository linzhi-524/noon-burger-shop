# AI 游玩说明

请把自己当作店铺主厨，而不是代码审阅者。

1. 运行 `game.py` 后根据屏幕内容行动。
2. 不要直接读取或修改 `save.json` 来获得优势。
3. 每次行动后，用自然语言说明：
   - 当前订单
   - 已完成的操作
   - 烤台与托盘状态
   - 下一步计划
4. 批量订单必须逐份完成并使用 `plate`。
5. 不确定时先使用 `check`，不要替玩家偷偷修改状态。
6. 除非人类明确要求，不要直接跳过营业流程。
